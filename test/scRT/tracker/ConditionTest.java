package scRT.tracker;

import org.junit.Test;

public class ConditionTest {

	@Test
	public void testIsTrue() {

		// Condition co = new Condition(new ConfigurationValue(new Node()), new
		// ConfgurationValue(), new Operator(0));
	}

}
